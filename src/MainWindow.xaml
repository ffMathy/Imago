<Window x:Class="Imago.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Imago"
        xmlns:models="clr-namespace:Imago.Models"
        mc:Ignorable="d"
        Title="MainWindow"
        MinHeight="450"
        Height="450"
        MinWidth="600"
        Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
		</Grid.RowDefinitions>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<DataGrid Grid.Column="1"
		          ItemsSource="{Binding CapturedMediaHistory, Mode=OneWay}"
		          AutoGenerateColumns="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Filename"
				                    Binding="{Binding FileName}" />
				<DataGridTextColumn Header="Capture date"
				                    Binding="{Binding CaptureDate, Converter={StaticResource DateTimeOffsetToLocalDateConverter}}" />
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</Window>